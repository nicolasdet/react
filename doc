//https://runkit.com/home   Sandox de nodeJS pour browser, j'ai trouver ça et je sais pas ou le noter...






SYNTAXE

(() => {}())    Cette synthaxe veut dire une fonction () => {}   + State less, autoExecution (function)()
 
...tab   =  explode(tab)

* appendChild * ajoute un noeud a la fin du dernier enfant (append en jquery)


// Ajouter un object react à windows qui lui même aurra un attribut createElement ( qui peut étre une fonction )
window.React = {
        createElement
    };
